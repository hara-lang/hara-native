package hara.lang.declaration;

/** Receiver category used by an annotated built-in protocol extension. */
public enum HaraProtocolTarget {
  JAVA_CLASS,
  NIL,
  FOREIGN,
  DEFAULT,
  BOOLEAN,
  NUMBER,
  CHARACTER,
  STRING
}
